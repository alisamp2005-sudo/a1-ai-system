# Архитектура AI-системы

Данный документ описывает общую архитектуру системы, стек технологий и взаимодействие компонентов.

## Стек технологий

| Слой | Технологии | Назначение |
|------|------------|------------|
| **Оборудование** | Mac Mini M4 Pro (64 ГБ RAM) | Локальный сервер инференса и БД |
| **LLM Backend** | Ollama, MLX | Управление моделями, аппаратное ускорение |
| **Модели (Текст)** | Qwen 2.5 32B (Q4), Llama 3.1 8B (Q4) | Интеллект агентов |
| **Модели (Медиа)** | Whisper Large v3, LLaVA 7B | Распознавание голоса, анализ фото |
| **Фреймворк AI** | LangGraph, LangChain | Маршрутизация, циклы агентов, RAG |
| **Backend** | Python 3.11+, FastAPI | API, бизнес-логика |
| **Task Engine** | Celery, Redis | Очереди задач, SLA-таймеры |
| **Базы данных** | PostgreSQL 16, ChromaDB | Хранение данных, векторизация (RAG) |
| **Frontend** | React, TypeScript, Tailwind | Веб-дашборд, Админка |
| **Интерфейсы** | Telegram Bot API (aiogram), Telegram Mini Apps | Взаимодействие с пользователями |
| **Инфраструктура** | Docker Compose, Nginx, Cloudflare Tunnel | Контейнеризация, сеть, удаленный доступ |

## Компоненты системы

Система состоит из нескольких ключевых модулей, взаимодействующих друг с другом:

1. **Telegram-шлюз:** Принимает сообщения (текст, голос, фото), транскрибирует аудио через Whisper и передает в Router.
2. **Router (Диспетчер):** На базе LangGraph. Классифицирует запрос, определяет приоритет (P0-P3) и маршрутизирует нужному агенту. Использует легкую модель (8B).
3. **AI-Агенты (11 шт.):** Выполняют профильную работу. Простые агенты используют 8B модель, сложные (Юрист, Финансист) — 32B.
4. **QA-Контролер:** Проверяет ответ агента перед отправкой пользователю. Если есть галлюцинации или ответ неполный — возвращает агенту на доработку.
5. **SLA-Контролер + Task Engine:** Создает задачу в БД, запускает Celery-таймеры. При истечении времени (50%, 80%, 100%, +24ч, +48ч, +72ч) запускает цепочку эскалации.
6. **RAG-пайплайн:** Tesseract OCR (для сканов) + nomic-embed-text (векторизация) + ChromaDB. Обеспечивает агентов (Юрист, Аналитик) контекстом из корпоративных документов.
7. **Веб-интерфейсы:** Дашборд для руководства (статистика по 22 объектам) и Админ-панель (управление правами, загрузка СНиП).

## Распределение памяти (RAM)

Мультимодельный подход позволяет оптимизировать использование 64 ГБ памяти Mac Mini:

| Модель / Сервис | RAM |
|----------------|:---:|
| Qwen 2.5 32B (Q4) | ~22 ГБ |
| Llama 3.1 8B (Q4) | ~5 ГБ |
| LLaVA 7B (Q4) | ~5 ГБ |
| Whisper Large v3 | ~3 ГБ |
| Инфраструктура (БД, ОС) | ~14 ГБ |
| **Итого (пик)** | **~49 ГБ** |
| **Свободно** | **~15 ГБ** |
